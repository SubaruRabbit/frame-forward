{
  "name": "frame-forward-contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "validate": "redocly lint openapi.yaml",
    "snapshot": "redocly bundle openapi.yaml --ext json --output openapi-baseline.json",
    "compatibility": "redocly bundle openapi.yaml --ext json --output openapi-current.json && node scripts/check-compatibility.mjs openapi-baseline.json openapi-current.json",
    "quality": "npm run validate && npm run compatibility && npm run test",
    "test": "node --test"
  },
  "devDependencies": {
    "@redocly/cli": "2.49.0"
  }
}
