# Workspace checks

Run all local project checks from the repository root:

Windows (PowerShell):

```powershell
.\scripts\check.ps1
```

Linux/macOS:

```sh
./scripts/check.sh
```

If the script is not executable after checkout, run `chmod +x scripts/check.sh` once.
