## 1. Retake comparison

- [x] 1.1 `contracts`: define retake-link creation and structured comparison response and verify all comparison categories are required.
- [x] 1.2 `frame-forward-server/evaluation`: create plan-bound owned shooting sessions, attach evaluations to them, persist same-session retake relationships and verify cross-user and cross-session links are rejected.
- [x] 1.3 `frame-forward-server/evaluation`: calculate score, plan-context and EXIF differences and verify deterministic comparison fixtures.
- [x] 1.4 `frame-forward-server/ai-workflow`: summarize visual problem changes and verify an unfixed primary problem is not marked improved solely by score rise.
- [x] 1.5 `frame-forward-app/features/shooting-session`: create/select a plan-bound shooting session, render before/after media and structured changes, and verify missing optional EXIF degrades cleanly.
