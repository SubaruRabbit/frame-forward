## 1. 契约质量与兼容性

- [x] 1.1 `contracts`：逐项分类 Redocly 警告并以已批准证据修正文档性问题；验证 `npm run validate` 无未批准警告。
- [x] 1.2 `contracts`：添加 OpenAPI 兼容性检查与基线快照；验证破坏性变更会失败。
- [x] 1.3 `contracts`：为路径响应、认证与 schema 引用增加契约测试；验证与现有服务端输出一致。
- [x] 1.4 `docs/constitution`：记录发现行为差异时的停止、规格和回滚流程；人工审查通过。
