## 1. App shell

- [x] 1.1 `frame-forward-app/app`: add the four top-level routes and verify a navigation test reaches each placeholder screen.
- [x] 1.2 `frame-forward-app/app`: add session-gated root routing and verify an invalid session opens the login route.
- [x] 1.3 `frame-forward-app/app`: persist only the top-level route key and verify restart restores a valid key and rejects an unknown key.
- [x] 1.4 `frame-forward-app/shared/components`: add common loading, empty, denied and failure states and verify component tests expose retry only when supplied.
