## 1. Project foundations

- [x] 1.1 `frame-forward-app`: initialize the React Native TypeScript Android project and verify its default typecheck passes.
- [x] 1.2 `frame-forward-server`: create the Java 21 and Spring Boot 3.5.8 Maven reactor with `bootstrap` and `common`, then verify `mvn test` passes.
- [x] 1.3 `contracts`: add the minimal OpenAPI document and verify it passes an OpenAPI validation command.

## 2. Repository checks

- [x] 2.1 `scripts`: add one local command that runs App, server, and contract checks, then verify a clean workspace completes successfully.
