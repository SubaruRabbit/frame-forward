## 1. Feature 边界迁移

- [ ] 1.1 `frame-forward-app/features/auth`：抽取认证 UseCase；验证认证状态和错误测试。
- [ ] 1.2 `frame-forward-app/features/photo-import`：抽取选择/上传 UseCase；验证取消、进度和重试测试。
- [ ] 1.3 `frame-forward-app`：执行 lint、typecheck、Jest；验证页面无存储/网络直接依赖。
