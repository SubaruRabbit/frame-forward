## 1. Portfolio query

- [ ] 1.1 `contracts`: define paged work list, filters, detail availability and favorite operations and verify partial-detail examples validate.
- [ ] 1.2 `frame-forward-server/portfolio`: build an owner-scoped work list with subject, camera and lens filters and verify cross-user rows never appear.
- [ ] 1.3 `frame-forward-server/portfolio`: assemble work detail from public module queries and verify absent EXIF, plan or evaluation returns availability flags.
- [ ] 1.4 `frame-forward-server/portfolio`: persist favorite state and verify toggling one work does not alter another.

## 2. Portfolio UI

- [ ] 2.1 `frame-forward-app/features/portfolio`: implement list, filters, favorite and detail views and verify empty and partial-data states render without blank screens.
