## 1. 分层迁移

- [ ] 1.1 `frame-forward-server/auth`：先添加失败架构与规则测试；验证当前直接 Mapper 依赖被捕获。
- [ ] 1.2 `frame-forward-server/auth`：迁移规则与持久化编排；验证模块测试和 API 回归通过。
- [ ] 1.3 `frame-forward-server/equipment`：迁移规则与持久化编排；验证模块测试和 API 回归通过。
- [ ] 1.4 `frame-forward-server`：执行架构、格式、覆盖率门禁；验证无反向依赖。
